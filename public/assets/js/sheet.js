import {escape as e} from './api.js';
import {attributes,tabs,ranks} from './catalog.js';
import {section,collectionRows} from './collections.js';
export const signed=n=>Number(n)>=0?'+'+n:String(n);
export function sheet(c,tab){const v=c.computed;return `<div class="heading"><div><p class="eyebrow">${e(c.ancestry||'ASCENDANCE À CHOISIR')} • NIVEAU ${c.level}</p>${c.portrait?`<img class="portrait" src="portrait.php?id=${c.id}&v=${c.revision}" alt="Portrait de ${e(c.name)}">`:""}<h1>${e(c.name)}</h1><p class="muted">${e(c.class||'Classe à choisir')}${c.heritage?' · '+e(c.heritage):''} ${c.campaign?' / '+e(c.campaign):''}</p></div><div class="toolbar"><button data-portrait>Portrait</button><button data-settings>✎ Personnage</button><button data-rest>☾ Repos</button><button data-mode>Mode combat</button><button data-print>Imprimer</button><a class="badge" href="api.php?action=export&id=${c.id}">Exporter JSON</a></div></div><div class="stats"><div class="stat"><small>POINTS DE VIE</small><strong>${c.hp}<small> / ${v.hp_max}</small></strong><small>${c.temp_hp} temporaires</small></div>${[['Armure','CLASSE D’ARMURE'],['Perception','PERCEPTION'],['Vigueur','VIGUEUR'],['Réflexes','RÉFLEXES'],['Volonté','VOLONTÉ']].map(([k,label])=>`<button class="stat" data-calc="${e(k)}"><small>${label}</small><strong>${k==='Armure'?v.stats[k].total:signed(v.stats[k].total)}</strong><small>Voir le calcul</small></button>`).join('')}</div><div class="toolbar no-print"><button data-hp="damage" class="danger">− Dégâts</button><button data-hp="heal">+ Soins</button><button data-hp="temporary">PV temporaires</button><button data-add="conditions">+ Condition</button><button data-add="modifiers">+ Modificateur</button><span class="badge">Vitesse ${v.speed} pieds</span>${c.hp===0?'<span class="warning">À 0 PV : vérifier mourant, blessé et les circonstances.</span>':''}</div><nav class="tabs" aria-label="Sections">${Object.entries(tabs).map(([k,label])=>`<button data-tab="${k}" class="${tab===k?'active':''}">${label}</button>`).join('')}</nav><div id="tab-content">${tabContent(c,tab)}</div>`;}
function attacks(c){return c.computed.attacks.length?c.computed.attacks.map(a=>`<div class="row"><div><strong>${e(a.name)}</strong><p>${e(a.damage)}</p></div><button data-attack="${a.id}">${a.map.map(signed).join(' / ')}</button></div>`).join(''):'<p class="muted">Équipez une arme dans l’inventaire pour afficher ses attaques.</p>';}
export function tabContent(c,tab){const v=c.computed;
 if(tab==='summary')return `<div class="two-col"><div><section class="panel"><div class="section-head"><h3>Attributs</h3><button data-attributes>Modifier</button></div><div class="attributes">${Object.entries(attributes).map(([k,label])=>`<div class="attribute"><small>${e(label)}</small><strong>${signed(v.attributes[k])}</strong></div>`).join('')}</div><div class="row"><span>DD de classe</span><button data-calc="Classe">${v.stats.Classe.total}</button><span>DD de sorts</span><button data-spell-dc>${v.spell_dc}</button></div></section><section class="panel"><h3>Attaques prêtes</h3>${attacks(c)}</section>${section(c,'resources','Ressources')}</div><div>${section(c,'conditions','Conditions actives')}${section(c,'modifiers','Modificateurs temporaires')}<section class="panel"><h3>Derniers événements</h3>${history(c,8)}</section></div></div>`;
 if(tab==='skills')return `<section class="panel"><div class="section-head"><h3>Compétences & maîtrises</h3><button data-lore>+ Connaissance</button></div><div class="table-wrap"><table><thead><tr><th>Compétence</th><th>Attribut</th><th>Maîtrise</th><th>Bonus</th><th></th></tr></thead><tbody>${c.skills.map(s=>`<tr><td>${e(s.name)}</td><td>${e(attributes[s.attribute])}</td><td>${ranks[s.rank]}</td><td><button data-calc="${e(s.name)}">${signed(v.stats[s.name]?.total??0)}</button></td><td><button data-skill="${s.id}">Modifier</button></td></tr>`).join('')}</tbody></table></div></section>`;
 if(tab==='combat')return `<div class="two-col"><div><section class="panel"><div class="section-head"><h3>Frappes & attaques multiples</h3><button data-round>Nouveau tour</button></div>${attacks(c)}<p class="small muted">La pénalité d’attaques multiples s’applique selon le trait agile. Les traits spéciaux et situations restent à vérifier.</p></section><section class="panel"><h3>Bouclier</h3><button data-shield>${c.shield_raised?'Abaisser':'Lever'} le bouclier</button><button data-block>Bloquer</button><p class="muted">Le blocage applique la dureté, puis les dégâts restants au personnage et au bouclier.</p></section><section class="panel"><h3>Sens, défenses & déplacements</h3>${Object.entries(c.details).map(([k,val])=>`<p><strong>${e(k)}</strong> : ${e(val)}</p>`).join('')||'<p class="muted">À renseigner dans les informations du personnage.</p>'}</section></div><div>${section(c,'conditions','Conditions')}${section(c,'modifiers','Modificateurs')}${section(c,'resources','Ressources')}</div></div>`;
 if(tab==='items')return `<div class="hero-banner"><div class="section-head"><div><p class="eyebrow">BOURSE & ÉQUIPEMENT</p><h2>${Math.floor(c.copper/1000)} pp · ${Math.floor(c.copper%1000/100)} po · ${Math.floor(c.copper%100/10)} pa · ${c.copper%10} pc</h2><p class="muted">Valeur totale ${(c.copper/100).toFixed(2)} po • conversion automatique</p></div><button data-transfer>Transférer</button><button data-currency class="primary">+ / − Transaction</button></div><span class="badge">Encombrement ${v.bulk} / ${v.bulk_limit} • Maximum ${v.bulk_max}</span>${v.encumbered?'<p class="warning">Encombré : maladroit 1 et vitesse réduite de 10 pieds.</p>':''}</div>${section(c,'items','Inventaire')}<section class="panel"><h3>Transactions</h3>${[...c.currency_transactions].reverse().map(t=>`<div class="row"><div>${e(t.name)}<p class="muted small">${e(t.created_at)}</p></div><strong>${signed(t.data.delta/100)} po</strong>${!t.data.reversed?`<button data-txundo="${t.id}">Annuler</button>`:'<span class="muted">Annulée</span>'}</div>`).join('')||'<p class="muted">Aucune transaction.</p>'}</section>`;
 if(tab==='spells')return `<div class="hero-banner"><div class="section-head"><div><p class="eyebrow">MAGIE</p><h2>Attaque ${signed(v.stats.Sorts.total)} · DD ${v.spell_dc}</h2></div><div class="toolbar"><span>Focus ${c.focus} / ${c.focus_max}</span><button data-focus="-1">−1</button><button data-focus="1">+1</button><button data-refocus>Refocaliser</button></div></div></div><div class="two-col"><div>${section(c,'spells','Grimoire & répertoire')}</div><div>${section(c,'spell_slots','Emplacements de sorts')}<p class="muted small">Les emplacements se consomment indépendamment des marques de préparation. Les tours de magie ne consomment pas d’emplacement.</p></div></div>`;
 if(tab==='progression')return `<div class="hero-banner"><p class="eyebrow">LE CHEMIN DU HÉROS</p><h2>Niveau ${c.level}</h2><p>${c.milestone?'Progression par jalons':`${c.xp} / ${c.xp_target} XP`}</p><div class="toolbar"><button data-xp>+ / − XP</button><button data-level class="primary" ${c.level>=20?'disabled':''}>Passer au niveau ${Math.min(20,Number(c.level)+1)}</button></div></div><section class="panel"><h3>Choix de chaque niveau</h3>${[...c.level_history].reverse().map(r=>`<div class="row"><div><strong>${e(r.name)}</strong><p class="prose">${e(typeof r.data.choices==='string'?r.data.choices:JSON.stringify(r.data.choices,null,2))}</p>${r.data.boosts?`<p>Boosts : ${e(r.data.boosts.join(', '))}</p>`:''}${r.data.before?`<button data-snapshot="${r.id}">Exporter l’état précédent</button>`:''}</div></div>`).join('')}</section><section class="panel"><h3>Historique des modifications</h3>${history(c,100)}</section>`;
 return section(c,tab,tabs[tab]);
}
function history(c,limit){return c.audit_history.slice(0,limit).map(r=>`<div class="row"><div class="small"><span class="muted">${e(new Date(r.created_at).toLocaleString('fr-FR'))}</span><p>${e(r.name)}</p></div>${r.data.undo&&!r.data.undone?`<button data-hpundo="${r.id}">Annuler</button>`:''}</div>`).join('')||'<p class="muted">Votre aventure commence ici.</p>';}
